/**
 * @deprecated Superseded by TicketPanelMessageEditor, which renders inline
 * (expands within the page content) instead of as a modal dialog. Kept as a
 * thin re-export in case this filename is still referenced anywhere.
 */
export { TicketPanelMessageEditor as TicketPanelMessageDrawer, EMPTY_PANEL_MESSAGE } from "./TicketPanelMessageEditor";
